# AC Infinity Fan Configuration

Use these PG3 custom parameters:

- `user`: AC Infinity account email or username.
- `password`: AC Infinity account password.
- `temp_unit`: set to `F` or `f` to display Fahrenheit. Any other value defaults to Celsius.

The node server uses internal defaults for the rest of the cloud settings.